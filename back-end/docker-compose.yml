services:
  mysql:
    container_name: mysql
    image: mysql:8
    restart: always
    env_file: ".env"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./init-scripts:/docker-entrypoint-initdb.d
    ports:
      - 3306:3306

volumes:
  mysql_data:
